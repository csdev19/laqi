{
  "backend-caido": {
    "GET /todos": "error",
    "POST /todos": "error",
    "PUT /todos/:id": "error",
    "DELETE /todos/:id": "error"
  },
  "usuario-nuevo": { "GET /todos": "empty" },
  "sesion-expirada": { "GET /profile": "unauthorized" },
  "red-lenta": { "GET /todos": "slow", "POST /auth/login": "slow" }
}
